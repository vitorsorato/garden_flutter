import 'package:flutter/material.dart';

class NewPlantsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Cadastro Nova Planta'),
          centerTitle: true,
          backgroundColor: Colors.green,
        ),
      );
}
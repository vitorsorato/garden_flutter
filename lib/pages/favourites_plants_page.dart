import 'package:flutter/material.dart';

class PlantsFavouritesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Plantas Favoritas'),
          centerTitle: true,
          backgroundColor: Colors.red,
        ),
      );
}